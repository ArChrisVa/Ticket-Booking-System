# Template for services/booking/.env — copy to ".env" and fill in real values.
# JWT_SECRET must be IDENTICAL to the auth service's secret so booking can verify
# tokens that auth issued (shared-secret / HS256 stateless auth).
JWT_SECRET=replace-with-the-same-secret-as-auth
